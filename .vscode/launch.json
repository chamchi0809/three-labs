{
  "version": "0.2.0",
  "configurations": [
    {
      // opens scene/demo as the workspace so scenes/*.tscene are picked up right away (the tscene link and three both resolve from there)
      "name": "tscene: Extension Host",
      "type": "extensionHost",
      "request": "launch",
      "args": ["${workspaceFolder}/scene/demo", "--extensionDevelopmentPath=${workspaceFolder}/scene/vscode"],
      "outFiles": ["${workspaceFolder}/scene/vscode/*.js"]
    },
    {
      // start the above first, then attach to hit breakpoints in lsp.ts (a debug extension host starts the server with --inspect=6009)
      "name": "tscene: Attach to language server",
      "type": "node",
      "request": "attach",
      "port": 6009,
      "restart": true,
      "skipFiles": ["<node_internals>/**"]
    }
  ],
  "compounds": [
    {
      "name": "tscene: Extension + server",
      "configurations": ["tscene: Extension Host", "tscene: Attach to language server"]
    }
  ]
}
