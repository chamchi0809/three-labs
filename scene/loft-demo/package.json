{
  "name": "loft-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "vite build --configLoader runner",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json",
    "check": "node --experimental-strip-types --disable-warning=ExperimentalWarning scenes.check.ts"
  },
  "dependencies": {
    "tscene": "workspace:*"
  }
}
